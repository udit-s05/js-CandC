"use strict"; // treat all js codes as newer version

let name = "Udit"
let age = 23
// supported data types :- int, string, boolean, null, undefined, symbol

console.log(typeof null);
console.log(typeof undefined);
